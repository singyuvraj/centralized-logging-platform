# Copy to .env and set values. .env is gitignored; do not commit it.
#
# Spring Boot Profile
# Options: dev, prod, staging, local, test
SPRING_PROFILES_ACTIVE=dev

# AWS Parameter Store Environment
# Determines which Parameter Store path to use: /config/suljhaoo-backend-service/{env}/application.json
# Defaults to 'dev' if not set
# Options: dev, prod, staging, etc.
AWS_PARAMETERSTORE_ENVIRONMENT=dev

# AWS Configuration
AWS_REGION=ap-south-1

# AWS Credentials (if using IAM user instead of IAM role)
# Uncomment and set if needed
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Phase 4: Elasticsearch (required for docker compose; set strong values in production)
ELASTIC_PASSWORD=changeme-elastic
LOGSTASH_WRITER_PASSWORD=changeme-logstash-writer
ELASTICSEARCH_HOST=elasticsearch

# Phase 5: Kibana (required for docker compose; set strong values in production)
KIBANA_SYSTEM_PASSWORD=changeme-kibana-system
KIBANA_USER_PASSWORD=changeme-kibana-user

